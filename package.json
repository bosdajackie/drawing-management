{
  "name": "drawing-management-software",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm run start",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  }
}
